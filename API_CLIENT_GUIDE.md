# 🐍 Python API Client Library\n\n## Installation\n\n```bash\npip install requests\n```\n\n## Quick Start\n\n```python\nfrom python_client import ATSResumeClient\n\n# Initialize client\nclient = ATSResumeClient(base_url=\"http://localhost:8000\")\n\n# Set API key\nclient.set_api_key(\"your_gemini_api_key_here\")\n\n# Upload and analyze resume\nresult = client.upload_resume(\"resume.pdf\")\nresume_data = result[\"parsed_resume\"]\n\n# Analyze against job description\nwith open(\"job_description.txt\") as f:\n    jd = f.read()\n\nanalysis = client.analyze_resume(resume_data, jd)\nprint(f\"ATS Score: {analysis['ats_score']}/100\")\n\n# Export optimized resume\nclient.export_resume(\n    resume_data, \n    jd, \n    format=\"pdf\",\n    output_path=\"optimized_resume.pdf\"\n)\n```\n\n## API Reference\n\n### Initialize Client\n\n```python\nfrom python_client import ATSResumeClient\n\nclient = ATSResumeClient(\n    base_url=\"http://localhost:8000\",\n    api_key=\"optional_key_here\"\n)\n```\n\n### Set API Key\n\n```python\nresponse = client.set_api_key(\"AIzaSyB...\")\nprint(response)  # Returns validation result\n```\n\n### Health Check\n\n```python\nhealth = client.health_check()\nprint(health)  # {\"status\": \"healthy\", \"message\": \"...\"}\n```\n\n### Upload Resume\n\n```python\nresult = client.upload_resume(\"path/to/resume.pdf\")\n\nprint(result[\"file_id\"])           # Unique file ID\nprint(result[\"parsed_resume\"])     # Parsed data\nprint(result[\"status\"])            # \"success\"\n```\n\n### Validate Resume\n\n```python\nvalidation = client.validate_resume(resume_data)\n\nprint(validation[\"completeness_score\"])  # 0-100\nprint(validation[\"sections\"])            # Section scores\nprint(validation[\"warnings\"])            # Missing items\n```\n\n### Analyze Resume\n\n```python\nanalysis = client.analyze_resume(\n    resume_data=resume_data,\n    job_description=\"Senior DevOps Engineer...\",\n    detailed=True\n)\n\nprint(f\"ATS Score: {analysis['ats_score']}\")\nprint(f\"Match: {analysis['match_percentage']}%\")\nprint(f\"Strengths: {analysis['strengths']}\")\nprint(f\"Weaknesses: {analysis['weaknesses']}\")\nprint(f\"Suggestions: {analysis['suggestions']}\")\n```\n\n### Skill Gap Analysis\n\n```python\ngaps = client.get_skill_gap(resume_data, job_description)\n\nprint(f\"Critical Missing: {gaps['critical_missing']}\")\nprint(f\"Recommended: {gaps['recommended_skills']}\")\nprint(f\"Gap %: {gaps['skill_gap_percentage']}\")\nprint(f\"Categories: {gaps['categories']}\")\n```\n\n### Section Validation\n\n```python\nsections = client.validate_sections(resume_data)\n\nprint(sections[\"section_scores\"])      # Individual scores\nprint(sections[\"completeness\"])        # Overall score\nprint(sections[\"missing_sections\"])    # Missing items\n```\n\n### Optimize Resume\n\n```python\noptimized = client.optimize_resume(\n    resume_data=resume_data,\n    job_description=job_description,\n    preserve_format=True\n)\n\nprint(optimized[\"optimized_resume\"])  # Optimized data\nprint(optimized[\"changes_made\"])       # What changed\n```\n\n### Export Resume\n\n```python\n# Export as PDF\npdf_bytes = client.export_resume(\n    resume_data=resume_data,\n    job_description=job_description,\n    format=\"pdf\",\n    theme=\"professional\",\n    output_path=\"output.pdf\"\n)\n\n# Export as DOCX\ndocx_bytes = client.export_resume(\n    resume_data=resume_data,\n    job_description=job_description,\n    format=\"docx\",\n    output_path=\"output.docx\"\n)\n\n# Export as Plain Text (ATS-friendly)\ntxt_bytes = client.export_resume(\n    resume_data=resume_data,\n    job_description=job_description,\n    format=\"plaintext\",\n    output_path=\"output.txt\"\n)\n\n# Export as Markdown\nmd_bytes = client.export_resume(\n    resume_data=resume_data,\n    job_description=job_description,\n    format=\"markdown\",\n    output_path=\"output.md\"\n)\n```\n\n### Batch Export\n\n```python\nfiles = client.batch_export(\n    resume_data=resume_data,\n    job_description=job_description,\n    formats=[\"pdf\", \"docx\", \"plaintext\", \"markdown\"]\n)\n\nprint(files[\"files\"])   # {\"pdf\": \"path\", \"docx\": \"path\", ...}\n```\n\n## Complete Example\n\n```python\nfrom python_client import ATSResumeClient\nimport json\n\n# 1. Initialize\nclient = ATSResumeClient(\"http://localhost:8000\")\nprint(\"✓ Client initialized\")\n\n# 2. Set API Key\nclient.set_api_key(\"your_gemini_api_key_here\")\nprint(\"✓ API key configured\")\n\n# 3. Check Health\nhealth = client.health_check()\nprint(f\"✓ API Status: {health['status']}\")\n\n# 4. Upload Resume\nprint(\"\\n📄 Uploading resume...\")\nupload_result = client.upload_resume(\"resume.pdf\")\nresume_data = upload_result[\"parsed_resume\"]\nprint(f\"✓ Resume uploaded: {upload_result['file_id']}\")\n\n# 5. Validate Resume\nprint(\"\\n✔️  Validating resume...\")\nvalidation = client.validate_resume(resume_data)\nprint(f\"✓ Completeness: {validation['completeness_score']}%\")\nif validation[\"warnings\"]:\n    print(f\"⚠️  Warnings: {validation['warnings']}\")\n\n# 6. Load Job Description\nprint(\"\\n📋 Loading job description...\")\nwith open(\"job_description.txt\") as f:\n    job_description = f.read()\nprint(f\"✓ Job description loaded ({len(job_description)} chars)\")\n\n# 7. Analyze Resume\nprint(\"\\n🔍 Analyzing resume against JD...\")\nanalysis = client.analyze_resume(resume_data, job_description)\nprint(f\"✓ ATS Score: {analysis['ats_score']}/100\")\nprint(f\"✓ Match: {analysis['match_percentage']}%\")\nprint(f\"✓ Matching Skills: {', '.join(analysis['matching_keywords'][:5])}...\")\nprint(f\"✓ Missing Skills: {', '.join(analysis['missing_keywords'][:5])}...\")\n\nprint(f\"\\nStrengths:\")\nfor s in analysis['strengths']:\n    print(f\"  ✓ {s}\")\n\nprint(f\"\\nWeaknesses:\")\nfor w in analysis['weaknesses']:\n    print(f\"  ✗ {w}\")\n\n# 8. Get Skill Gaps\nprint(\"\\n🎓 Analyzing skill gaps...\")\ngaps = client.get_skill_gap(resume_data, job_description)\nprint(f\"✓ Gap Percentage: {gaps['skill_gap_percentage']:.1f}%\")\nprint(f\"✓ Critical Missing: {', '.join(gaps['critical_missing'][:3])}\")\nprint(f\"✓ Recommended: {', '.join(gaps['recommended_skills'][:3])}\")\n\n# 9. Optimize Resume\nprint(\"\\n⚡ Optimizing resume...\")\noptimized_result = client.optimize_resume(resume_data, job_description)\noptimized_data = optimized_result[\"optimized_resume\"]\nprint(f\"✓ Resume optimized\")\nprint(f\"✓ Changes: {optimized_result['changes_made']}\")\n\n# 10. Export Formats\nprint(\"\\n💾 Exporting in multiple formats...\")\n\n# PDF\nclient.export_resume(\n    optimized_data, job_description, \n    format=\"pdf\", output_path=\"output/resume.pdf\"\n)\nprint(\"✓ PDF exported\")\n\n# DOCX\nclient.export_resume(\n    optimized_data, job_description,\n    format=\"docx\", output_path=\"output/resume.docx\"\n)\nprint(\"✓ DOCX exported\")\n\n# Plain Text\nclient.export_resume(\n    optimized_data, job_description,\n    format=\"plaintext\", output_path=\"output/resume.txt\"\n)\nprint(\"✓ Plain text exported\")\n\n# Markdown\nclient.export_resume(\n    optimized_data, job_description,\n    format=\"markdown\", output_path=\"output/resume.md\"\n)\nprint(\"✓ Markdown exported\")\n\nprint(\"\\n✅ All tasks completed!\")\nprint(\"📂 Check output/ directory for results\")\n```\n\n## Error Handling\n\n```python\nfrom python_client import ATSResumeClient\nimport requests\n\nclient = ATSResumeClient(\"http://localhost:8000\")\n\ntry:\n    # Try to upload resume\n    result = client.upload_resume(\"resume.pdf\")\nexcept FileNotFoundError as e:\n    print(f\"Error: {e}\")\n    print(\"Make sure resume.pdf exists\")\nexcept requests.exceptions.ConnectionError:\n    print(\"Error: Cannot connect to API\")\n    print(\"Make sure API is running at http://localhost:8000\")\nexcept requests.exceptions.HTTPError as e:\n    print(f\"API Error: {e}\")\n    print(\"Check the error response for details\")\n```\n\n## Common Tasks\n\n### Task 1: Batch Process Multiple Resumes\n\n```python\nimport os\nfrom pathlib import Path\n\nclient = ATSResumeClient(\"http://localhost:8000\")\nclient.set_api_key(\"your_key_here\")\n\nresume_dir = \"resumes/\"\njob_description = open(\"jd.txt\").read()\n\nresults = []\n\nfor resume_file in Path(resume_dir).glob(\"*.pdf\"):\n    print(f\"Processing {resume_file.name}...\")\n    \n    # Upload\n    upload = client.upload_resume(str(resume_file))\n    resume_data = upload[\"parsed_resume\"]\n    \n    # Analyze\n    analysis = client.analyze_resume(resume_data, job_description)\n    \n    results.append({\n        \"file\": resume_file.name,\n        \"ats_score\": analysis[\"ats_score\"],\n        \"match\": analysis[\"match_percentage\"]\n    })\n\n# Sort by score\nresults.sort(key=lambda x: x[\"ats_score\"], reverse=True)\n\nprint(\"\\nResults (sorted by ATS score):\")\nfor i, r in enumerate(results, 1):\n    print(f\"{i}. {r['file']}: {r['ats_score']}/100 ({r['match']}% match)\")\n```\n\n### Task 2: Compare Multiple Job Descriptions\n\n```python\nclient = ATSResumeClient(\"http://localhost:8000\")\nclient.set_api_key(\"your_key_here\")\n\nresume_data = client.upload_resume(\"resume.pdf\")[\"parsed_resume\"]\n\njob_descriptions = {\n    \"job1.txt\": \"Senior DevOps Engineer at TechCorp\",\n    \"job2.txt\": \"DevOps Engineer at StartupXYZ\",\n    \"job3.txt\": \"Cloud Infrastructure Engineer at BigCorp\"\n}\n\ncomparison = {}\n\nfor job_name, jd_path in job_descriptions.items():\n    with open(jd_path) as f:\n        jd = f.read()\n    \n    analysis = client.analyze_resume(resume_data, jd)\n    comparison[job_name] = analysis[\"ats_score\"]\n\nbest_match = max(comparison, key=comparison.get)\nprint(f\"Best match: {best_match} ({comparison[best_match]}/100)\")\n```\n\n### Task 3: Generate Optimization Report\n\n```python\nclient = ATSResumeClient(\"http://localhost:8000\")\nclient.set_api_key(\"your_key_here\")\n\nresume_data = client.upload_resume(\"resume.pdf\")[\"parsed_resume\"]\njob_description = open(\"jd.txt\").read()\n\n# Generate report\nanalysis = client.analyze_resume(resume_data, job_description)\ngaps = client.get_skill_gap(resume_data, job_description)\n\nreport = f\"\"\"\n╔══════════════════════════════════════════════════════════════╗\n║          ATS RESUME OPTIMIZATION REPORT                      ║\n╚══════════════════════════════════════════════════════════════╝\n\n📊 SCORES\n  • ATS Score: {analysis['ats_score']}/100\n  • Match Percentage: {analysis['match_percentage']}%\n\n✅ STRENGTHS\n{chr(10).join(f\"  • {s}\" for s in analysis['strengths'])}\n\n❌ WEAKNESSES\n{chr(10).join(f\"  • {w}\" for w in analysis['weaknesses'])}\n\n🎯 MISSING SKILLS (Critical)\n{chr(10).join(f\"  • {s}\" for s in gaps['critical_missing'])}\n\n💡 RECOMMENDATIONS (Nice-to-have)\n{chr(10).join(f\"  • {s}\" for s in gaps['recommended_skills'])}\n\n💬 SUGGESTIONS\n{chr(10).join(f\"  • {s}\" for s in analysis['suggestions'][:3])}\n\n📈 SKILL GAP: {gaps['skill_gap_percentage']:.1f}%\n\"\"\"\n\nprint(report)\n\n# Save report\nwith open(\"optimization_report.txt\", \"w\") as f:\n    f.write(report)\n```\n\n## Troubleshooting\n\n### Connection Errors\n\n```python\n# Make sure API is running\n# http://localhost:8000/docs should be accessible\n\n# Test connection\ntry:\n    client = ATSResumeClient(\"http://localhost:8000\")\n    health = client.health_check()\n    print(\"✓ Connection successful\")\nexcept Exception as e:\n    print(f\"✗ Connection failed: {e}\")\n```\n\n### Invalid API Key\n\n```python\n# Make sure you have a valid Gemini API key\n# Get one from: https://aistudio.google.com/app/apikey\n\ntry:\n    client.set_api_key(\"your_key_here\")\nexcept Exception as e:\n    print(f\"Invalid API key: {e}\")\n```\n\n### File Not Found\n\n```python\nimport os\n\n# Check file exists\nfile_path = \"resume.pdf\"\nif not os.path.exists(file_path):\n    print(f\"File not found: {file_path}\")\n    print(f\"Current directory: {os.getcwd()}\")\n    print(f\"Files in directory: {os.listdir('.')}\")\n```\n\n## Next Steps\n\n- 📖 Read full API documentation: http://localhost:8000/docs\n- 🚀 Deploy to production: See PRODUCTION_DEPLOYMENT.md\n- 🧪 Run tests: `pytest tests/`\n- 📝 Create custom integrations\n